name: CI

on: [push]
jobs:
    build-test:
        run-on: ubuntu-20.04
        steps:

            - name: Checkout
              uses: actions/checkout@v3

            - name: Cypress Tests
              uses: cypress-io/github-actions@v5
              with:
                start: 'npm run dev'
                build: 'npm run build'